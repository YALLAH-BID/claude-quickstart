# LF in the repository and in the working copy on every platform.
# Explicit eol=lf overrides core.autocrlf (set to true globally here),
# which otherwise converts on checkout and produces the
# "LF will be replaced by CRLF" warnings on Windows.
* text=auto eol=lf
